import { NomineeCard, PublicFooter, PublicHeader, Pill } from '@/components/foundation'
import { nominees } from '@/lib/mock-data'
export default async function NomineeProfilePage({ params }: { params: Promise<{ code: string }> }) { const { code } = await params; const nominee = nominees.find(n => n.initials.toLowerCase() === code.toLowerCase()) || nominees[0]; return <><PublicHeader/><main className="section"><Pill tone="coral">Nominee profile · {nominee.initials}</Pill><h1>{nominee.name}&apos;s <em>story.</em></h1><NomineeCard nominee={nominee}/><a className="button" href={`/vote/${nominee.initials.toLowerCase()}`}>Vote for {nominee.name.split(' ')[0]}</a></main><PublicFooter/></> }
