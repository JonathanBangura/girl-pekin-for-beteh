import { PublicHeader, PublicFooter, NomineeCard, Pill } from '@/components/public/public'
import { nominees } from '@/lib/mock-data'
export default function NomineesPage() { return <><PublicHeader/><main className="section"><Pill tone="coral">2026 nominee directory · demo data</Pill><h1>Meet the <em>nominees.</em></h1><p>Search and explore the students featured in this frontend demo.</p><div className="filter-row"><input aria-label="Search nominees" placeholder="Search by name or code"/><select aria-label="Category filter"><option>All mock categories</option><option>Community Champion</option><option>Future Maker</option></select></div><div className="nominee-grid">{nominees.map(n => <div key={n.code}><NomineeCard nominee={n}/><p className="eyebrow">{n.code} · {n.institution}</p></div>)}</div></main><PublicFooter/></> }
